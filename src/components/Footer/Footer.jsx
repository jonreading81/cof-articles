import React from 'react';

import * as styles from './styles';

export const Footer = ({ links }) => (
  <section css={styles.footer}>
    <p className="text-xs">Copyright © Call of the Forest 2020</p>
  </section>
);
