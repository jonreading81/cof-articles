{
  "name": "cof-articles",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "upload": "aws s3 sync out s3://articles.calloftheforest.com",
    "deploy": "yarn build && yarn export && yarn upload"
  },
  "dependencies": {
    "@emotion/react": "^11.0.0",
    "aws": "^0.0.3-2",
    "gray-matter": "^4.0.2",
    "marked": "^0.8.2",
    "next": "^10.0.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
